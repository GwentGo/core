import * as monsters from './monsters'
import * as skellige from './skellige'
import * as neutral from './neutral'
import * as derivatives from './derivatives'

export const { eredin, wild_hunt_hound, ice_giant, crone__brewess, crone__weavess, crone__whispess, wild_hunt_warrior, wild_hunt_navigator, wild_hunt_longship, drowner, slyzard, frightener } = monsters
export const { clan_brokvar_archer } = skellige
export const { biting_frost, swallow_potion, muzzle, white_frost, geralt__igni } = neutral
export const { frost_hazard } = derivatives
